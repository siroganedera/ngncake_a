<div class='container mt-3'>
  <div class="text-center"><%= flash[:notice] %></div>
  <div class='row'>
    <div class='col-md-2'>
      
    <%# ジャンル検索・一覧 （見た目だけ）%>
      <table class='table table-hover table-inverse'>
        <thead>
          <tr>
            <th>ジャンル検索</th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
            <tr>
             <td><% genre.name %></td>
            </tr>
          <% end %>
        </tbody>  
      </table>
    </div>
    <div class='col-md-7 offset-md-3 m-auto'>
      <h4>ようこそ、ながのCAKEへ！<br>
      このサイトは、ケーキ販売のECサイトになります。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。</h4>
    </div>
  </div>
  <div class="mx-auto">
  <h2>新着商品</h2>
    <div class="row">
      <% @items.first(4).each do |item| %>
        <div class="col-md-3">
          <div class="card" style="width: 18rem;">
            <%= link_to item_path(item) do %>
            <%= image_tag item.get_image, size: "250x250",class:"card-img-top"  %>
            <% end %>
            <div class="card-body">
              <p class="card-text"><%= item.name %></p>
              <% if item.is_active == true %>
                <p class="card-text"><%= item.price %>円</p>
              <% else %>
                <p class="card-text text-danger">販売停止中</p>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <div class="text-right m-4">
      <%= link_to "すべての商品を見る", items_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>