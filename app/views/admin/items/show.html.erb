<div class='container mt-4'>
  <h2 class="offset-md-2 mt-4">商品詳細</h2>
  <div class="row justify-content-center">
    <div class='col-md-4 p-5'>
      <%= image_tag @item.get_image(300,300) %>
    </div>
    <div class="col-md-8 p-5">
      <div class="d-flex">
        <p class="w-25 p-2">商品名</p><p class="p-2" style="width: 500px;"><%= @item.name %></p>
      </div>
      <div class="d-flex">
        <p class="w-25 p-2">商品説明</p><p class="p-2" style="width: 500px;"><%= @item.introduction %></p>
      </div>
      <div class="d-flex">
        <p class="w-25 p-2">ジャンル</p><p class="p-2" style="width: 500px;"><%= @item.genre.name %></p>
      </div>
      <div class="d-flex">
        <p class="w-25 p-2">税込価格<br>(税抜価格)</p><p class="p-2" style="width: 500px;"><%= @item.add_tax_price.to_s(:delimited) %>（<%= @item.price.to_s(:delimited) %>）円</p>
      </div>
      <div class="d-flex">
        <p class="w-25 p-2">
          <% if @item.is_active? %>
            販売ステータス<p class="p-2 text-success">販売中</p>
          <% else %>
            販売ステータス<p class="p-2 text-danger">販売停止中</p>
          <% end %>
        </p>
      </div>
    </div>
      <div class='row justify-content-center'>
        <p><%= link_to "編集する", edit_admin_item_path(@item.id),class: "btn btn-lg btn-success" %></p>
      </div>
  </div>
</div>