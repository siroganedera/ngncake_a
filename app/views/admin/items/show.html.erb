<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
    <h2>商品詳細</h2>
      <% if @item.image.attached? %>
        <%= image_tag @item.get_image, size: "300×300" %>
      <% else %>
        <%= image_tag 'no-image-icon', size: "300×300" %>
      <% end %>
      <p>商品名<%= @item.name %></P>
      <p>商品説明<%= @item.introduction %></p>
      <p>ジャンル<%= @item.genre.name %></p>
      <p>税込価格(税抜価格)<%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)</p>
      <p>
        <% if @item.is_active? %>
          <p class="badge badge-success">販売中</p>
        <% else %>
          <p class="badge badge-secondary">販売停止中</p>
        <% end %>
      </p>
      <p><%= link_to "編集する", edit_admin_item_path(@item.id),class: "btn btn-sm btn-success" %></p>
    </div>
  </div>
</div>