<div class='container'>
  <div class='row'>
    <h2>商品詳細</h2>
    <div>
      <%# image_tag @item.get_item_image, size: "300×300" %>
    </div>
    <div class='flex flex-col'>
      <p>商品名<%= @item.name %></P>
      <p>商品説明<%= @item.introduction %></p>

      <p>税込価格<br>(税抜価格)</p><p><%= @item.add_tax_price.to_s(:delimited) %><%= (@item.price) %></p>

    </div>
  </div>
</div>